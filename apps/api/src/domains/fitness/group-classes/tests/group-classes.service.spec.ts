import { GroupClassesService } from '../service/group-classes.service.js';

describe('GroupClassesService', () => {
  it('should be defined', () => {
    const service = new GroupClassesService();
    expect(service).toBeDefined();
  });
});
